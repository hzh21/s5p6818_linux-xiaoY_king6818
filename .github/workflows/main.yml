name: Build Optimized King6818 Kernel

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install GCC 10 Toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            gcc-10-aarch64-linux-gnu \
            g++-10-aarch64-linux-gnu \
            device-tree-compiler \
            bc libssl-dev make \
            bison flex libelf-dev \
            pkg-config curl

      - name: Patch TCP BBR (v1 Backport)
        run: |
          # 下载源码
          curl -sSL "https://git.kernel.org/pub/scm/linux/kernel/git/davem/net-next.git/plain/net/ipv4/tcp_bbr.c" -o net/ipv4/tcp_bbr.c
          
          if [ ! -s net/ipv4/tcp_bbr.c ]; then
            echo "BBR source download failed!"
            exit 1
          fi

          # 修正 Makefile 注入语法，避免 $ 符号引起 YAML 报错
          sed -i '/obj-$(CONFIG_TCP_CONG_ADVANCED)/a obj-$(CONFIG_TCP_CONG_BBR) += tcp_bbr.o' net/ipv4/Makefile
          
          # 修改 Kconfig
          if ! grep -q "config TCP_CONG_BBR" net/ipv4/Kconfig; then
            cat >> net/ipv4/Kconfig <<EOF
config TCP_CONG_BBR
    tristate "TCP BBR congestion control"
    default n
    help
      This enables TCP BBR congestion control algorithm.
EOF
          fi

      - name: Configure and Build
        run: |
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CC=aarch64-linux-gnu-gcc-10
          export ARCH=arm64
          
          # 1. 生成基础配置
          make $ARCH CROSS_COMPILE=$CROSS_COMPILE CC=$CC king6818_linux_defconfig
          
          # 2. 内核协议栈优化 (针对高并发视频和银行 App)
          ./scripts/config --enable CONFIG_TCP_CONG_BBR
          ./scripts/config --set-val CONFIG_DEFAULT_TCP_CONG "\"bbr\""
          ./scripts/config --enable CONFIG_NET_SCH_FQ
          ./scripts/config --set-val CONFIG_HZ 1000
          ./scripts/config --enable CONFIG_HZ_1000
          ./scripts/config --enable CONFIG_HIGH_RES_TIMERS
          ./scripts/config --enable CONFIG_SLUB
          ./scripts/config --disable CONFIG_SLUB_DEBUG
          
          # 3. 内存与 ZRAM 优化 (解决 2GB 内存 Swap 瓶颈)
          ./scripts/config --enable CONFIG_ZRAM
          ./scripts/config --enable CONFIG_ZRAM_LZ4_COMPRESS
          ./scripts/config --enable CONFIG_ZSMALLOC
          
          # 4. 网络分流支持
          ./scripts/config --enable CONFIG_RPS
          ./scripts/config --enable CONFIG_RFS
          
          # 5. 自动同步依赖
          make $ARCH CROSS_COMPILE=$CROSS_COMPILE CC=$CC olddefconfig
          
          # 6. 开始编译 (针对 Cortex-A53 开启性能模式)
          make $ARCH CROSS_COMPILE=$CROSS_COMPILE CC=$CC \
               KCFLAGS="-O3 -march=armv8-a -mtune=cortex-a53 -Wno-array-bounds -Wno-format-overflow" \
               Image dtbs -j$(nproc)

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Optimized-King6818-Kernel
          path: |
            arch/arm64/boot/Image
            arch/arm64/boot/dts/nexell/s5p6818-king6818.dtb
